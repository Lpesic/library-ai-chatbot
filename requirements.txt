requests==2.31.0
beautifulsoup4==4.12.3
pandas==2.1.4
lxml==5.1.0
python-dotenv==1.0.0
uvicorn[standard]==0.24.0

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pip install -r requirements.txt && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10